import React from 'react';
import { TTBadge } from '../design-system/components/TTBadge';

/**
 * StatusBadge - Adaptador hacia TTBadge TECTODE ERP
 */
export default function StatusBadge(props) {
  return <TTBadge {...props} />;
}
